/**
 * Placeholder — Navbar component will be implemented here.
 * It will include app branding, board switcher, and user actions.
 */
const Navbar = () => {
  return (
    <header className="w-full bg-gray-900 text-white px-6 py-4 flex items-center justify-between shadow-md">
      <h1 className="text-xl font-bold tracking-tight">📋 Kanban</h1>
      <span className="text-sm text-gray-400">Workspace</span>
    </header>
  );
};

export default Navbar;
