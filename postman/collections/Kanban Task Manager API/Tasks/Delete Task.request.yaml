$kind: http-request
name: Delete Task
method: DELETE
url: '{{baseUrl}}/api/tasks/{{taskId}}'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      // Implement when task routes are active
